# 快速使用指南

## ✅ 已完成

插件已经成功配置并可以使用了！

## 🚀 如何使用

### 方法 1: 直接启动（推荐）

```bash
cd /Users/trry/6bt/project/xiaoyaojing-platform/web/pc
opencode
```

启动后，按 `Tab` 键，你应该只看到：
- ✅ `cline-plan` (计划模式)
- ✅ `cline-act` (执行模式)

### 方法 2: 指定 Agent 启动

```bash
# 使用计划模式
opencode --agent cline-plan

# 使用执行模式  
opencode --agent cline-act
```

## 📝 Cline 模式说明

### cline-plan (计划模式)
- ✅ 分析代码库
- ✅ 创建详细的实施计划
- ❌ 不会修改任何文件
- ❌ 不会执行任何命令

### cline-act (执行模式)
- ✅ 按步骤执行计划
- ✅ 可以修改代码
- ✅ 可以执行命令
- ✅ 报告执行进度

## 🎯 工作流程示例

```bash
# 1. 进入计划模式
opencode --agent cline-plan

# 在 TUI 中描述你的需求
# "我想添加软删除功能"

# AI 会创建详细计划，包括：
# - 需要修改的文件列表
# - 实施步骤
# - 风险评估
# - 验证步骤

# 2. 审核计划
# 提问问题、要求修改

# 3. 切换到执行模式
# 按 Tab 键，选择 cline-act

# 4. 执行计划
# "请按计划执行"

# AI 会逐步执行：
# ✅ Step 1/8: 更新 Note Model
# ✅ Step 2/8: 创建数据库迁移
# ...
```

## ⚙️ 配置

当前配置在 `/Users/trry/.config/opencode/opencode.json` 中。

### 修改默认 Agent

编辑配置文件：

```json
{
  "agent": {
    "cline-plan": { ... },
    "cline-act": { ... }
  },
  "default_agent": "cline-plan"
}
```

### 保留默认 Agents

如果你想同时使用 OpenCode 的默认 agents：

```json
{
  "agent": {
    "cline-plan": { ... },
    "cline-act": { ... },
    "plan": { ... },
    "build": { ... }
  },
  "replace_default_agents": false
}
```

## 🔧 故障排除

### 看不到 cline agents？

```bash
# 检查配置
cat ~/.config/opencode/opencode.json | grep cline

# 列出所有 agents
opencode agent list
```

### Plan 模式仍然可以修改代码？

说明：计划创建时的可能没有正确加载 system prompt。

## 📚 更多信息

- 完整文档：`/Users/trry/6bt/myproject/opencode-cline-mode/README.md`
- GitHub：https://github.com/trry-hub/opencode-cline-mode

## 🎉 现在就开始使用吧！

启动 OpenCode，享受 Cline 风格的工作流程！
